
target create $_CHIPNAME.cpu8 cortex_m -dap $_CHIPNAME.dap -ap-num 8

$_CHIPNAME.cpu3 configure -event examine-end {
    enable_clock 5
}

$_CHIPNAME.cpu3 configure -event reset-end {
    enable_clock 5
}
